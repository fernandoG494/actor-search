import axios from 'axios';

export const getNameFromImage = (raw) => {
    // console.log("raw => ", raw)

    // const fmData = new FormData();
    // fmData.append("file", raw);

    // console.log("FmData => ", fmData)

    // axios({
    //     method: "post",
    //     url: process.env.REACT_APP_API_URL,
    //     headers: {
    //         'Nomada': process.env.REACT_APP_API_KEY,
    //         'content-type': 'multipart/form-data'
    //     },
    //     data: fmData
    // }).then(
    //     res => console.log("response => ", res)
    // ).catch(
    //     error => console.log("Error in petition: ", error)
    // );
}
